#include "stdafx.h"
#include "Animation.h"

bool Animation::attackLeft(sf::Sprite obj)
{
	if (elapsedTime < 0.5) {

	}
	else if (elapsedTime < 1.0);
	return false;
}

bool Animation::attackRight(sf::Sprite obj)
{
	return false;
}

bool Animation::buf(sf::Sprite obj)
{
	return false;
}
